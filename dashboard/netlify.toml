[build]
  publish = "site"

# data.json 은 배포마다 갱신되는 스냅샷 — 브라우저 캐시 금지
[[headers]]
  for = "/data.json"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
